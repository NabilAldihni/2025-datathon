## Available targets

- `dev`  
    Starts the application in development mode

## Usage

- Run the target:
    ```
    make dev
    ```

- Override default port (3000)
    ```
    make dev PORT=3001
    ```

- Preview the commands that will run without executing them:
    ```
    make -n dev
    ```